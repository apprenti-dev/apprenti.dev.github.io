---
import Layout from '../layouts/main.astro';
import { Home, ArrowLeft, Compass } from 'lucide-react';

const base = import.meta.env.BASE_URL;
---

<Layout title="Page Not Found — apprenti.dev">
	<section class="py-20 md:py-32 min-h-[60vh] flex items-center">
		<div class="container mx-auto px-4">
			<div class="max-w-2xl mx-auto text-center">
				{/* 404 Illustration */}
				<div class="mb-8">
					<div class="relative">
						<h1 class="text-9xl md:text-[200px] font-bold bg-gradient-to-r from-apprenti-violet to-apprenti-magenta bg-clip-text text-transparent opacity-20">
							404
						</h1>
						<div class="absolute inset-0 flex items-center justify-center">
							<div class="relative">
								<div class="absolute inset-0 bg-gradient-to-br from-apprenti-violet to-apprenti-magenta rounded-full blur-2xl opacity-30 animate-pulse"></div>
								<Compass class="h-24 w-24 md:h-32 md:w-32 text-apprenti-violet-on-dark relative" />
							</div>
						</div>
					</div>
				</div>

				{/* Content */}
				<h2 class="text-3xl md:text-4xl font-bold mb-4">
					This path doesn't exist — yet.
				</h2>
				<p class="text-lg text-muted-foreground mb-8">
					The page you're looking for may have moved or never existed.
					Let's get you back on track.
				</p>

				{/* Actions */}
				<div class="flex flex-col sm:flex-row gap-4 justify-center">
					<a
						href={base}
						class="inline-flex items-center justify-center gap-2 h-10 px-6 rounded-md bg-primary text-primary-foreground font-medium hover:bg-primary/90 transition-colors"
					>
						<Home class="h-4 w-4" />
						Back to apprenti.dev
					</a>
					<button
						onclick="history.back()"
						class="inline-flex items-center justify-center gap-2 h-10 px-6 rounded-md border bg-background hover:bg-accent hover:text-accent-foreground transition-colors"
					>
						<ArrowLeft class="h-4 w-4" />
						Go Back
					</button>
				</div>

				{/* Helpful Links */}
				<div class="mt-12 pt-8 border-t">
					<p class="text-sm text-muted-foreground mb-4">
						Here are some helpful links instead:
					</p>
					<div class="flex flex-wrap gap-4 justify-center">
						<a href={`${base}#features`} class="text-sm text-primary hover:underline">
							Features
						</a>
						<a href={`${base}docs`} class="text-sm text-primary hover:underline">
							Docs
						</a>
						<a href={`${base}about`} class="text-sm text-primary hover:underline">
							About
						</a>
						<a href={`${base}contact`} class="text-sm text-primary hover:underline">
							Contact
						</a>
						<a href={`${base}#faq`} class="text-sm text-primary hover:underline">
							FAQ
						</a>
					</div>
				</div>
			</div>
		</div>
	</section>
</Layout>
